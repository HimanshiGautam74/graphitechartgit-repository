apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphitechart.fullname" . }}-test-connection"
  labels:
    {{- include "graphitechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graphitechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
