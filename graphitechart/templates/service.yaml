apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphitechart.fullname" . }}
  labels:
    {{- include "graphitechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graphitechart.selectorLabels" . | nindent 4 }}
